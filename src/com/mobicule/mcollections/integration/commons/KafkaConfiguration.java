package com.mobicule.mcollections.integration.commons;

/*import java.util.HashMap;
import java.util.Map;

import org.apache.kafka.clients.producer.KafkaProducer;
import org.apache.kafka.clients.producer.ProducerConfig;
import org.apache.kafka.common.serialization.StringSerializer;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
*/






//@Configuration
public class KafkaConfiguration {

	/*
	 * @Bean public KafkaProducer<String, String> kafkaProducer() { Map<String,
	 * Object> configProps = new HashMap<String, Object>();
	 * 
	 * configProps.put(ProducerConfig.BOOTSTRAP_SERVERS_CONFIG, "localhost:9092");
	 * configProps.put(ProducerConfig.CLIENT_ID_CONFIG, "coll03");
	 * configProps.put(ProducerConfig.KEY_SERIALIZER_CLASS_CONFIG,
	 * StringSerializer.class);
	 * configProps.put(ProducerConfig.VALUE_SERIALIZER_CLASS_CONFIG,
	 * StringSerializer.class); return new KafkaProducer<>(configProps); }
	 */

	
}
